# Copy this file to .env and fill in your values

# AI config (private)
# Option A: JSON file path (recommended)
AI_CONFIG_PATH=.private/ai-config.json

# Option B: inline JSON (escaped)
# AI_CONFIG_JSON={"models":{...}}

# Port for the local server
PORT=3000

# Allowed CORS origins (comma-separated). Leave empty to allow any origin.
ALLOWED_ORIGIN=https://your-frontend-domain.com

# Signed URL TTL for private uploads (seconds)
UPLOAD_URL_TTL_SECONDS=3600

# Supabase project credentials
# Get these from: https://supabase.com/dashboard/project/_/settings/api
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your_anon_key_here
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# Stripe (optional — for subscription support page)
# Create a product + price at https://dashboard.stripe.com
STRIPE_SECRET_KEY=sk_test_...
STRIPE_PRICE_ID=price_...

# Dev accounts (comma-separated) — unlocks dev-only features
DEV_EMAILS=dev1@example.com,dev2@example.com

# Dev badge roles — JSON map of email → array of badge labels
# e.g. {"you@example.com":["OWNER","DEV"],"other@example.com":["DEV"]}
DEV_BADGE_CONFIG={}
